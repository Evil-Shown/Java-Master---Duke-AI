{
  "name": "java-academy",
  "private": true,
  "scripts": {
    "prepare": "node scripts/install-graphify-hook.js",
    "dev": "concurrently -k \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
